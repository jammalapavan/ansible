- name: install the nginx
  hosts: frontend
  tasks :
   - name: install nginx server
     ansible.builtin.package:
      name: nginx 
      state: present 

   - name: start nginx 
     ansible.builtin.service:
      name: nginx 
      state : present 
      enabled: yes  

